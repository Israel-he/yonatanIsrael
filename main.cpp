 
#include <iostream>
#include "gameControler.h"
 
using namespace std;
 
int main()
{
    gameControler a("BoardD.txt");
    
    a.print();



     

}


 





























       /* auto file = std::ifstream("Board.txt");
        Location loc = { 3,3 };
        Screen::setLocation(loc);
        cout << "R";
        std::this_thread::sleep_for(5s);
        int key = _getch();
        if (SpecialKeys::UP)
        {
            loc.row -= 1;
            Screen::setLocation(loc);
            cout << "R";
            std::this_thread::sleep_for(5s);
        }
        int key = _getch();
        if (SpecialKeys::DOWN)
        {
            loc.row += 1;
            Screen::setLocation(loc);
            cout << "R";
            std::this_thread::sleep_for(5s);
        }*/
         
    





























    /* auto file = std::ifstream("Board.txt");
        auto loc = Location();

        loc.col = _getch();
        loc.row = _getch();
        Screen::setLocation(loc);
        std::this_thread::sleep_for(5s);

        loc.col = _getch();
        loc.row = _getch();
        Screen::setLocation(loc);
        std::this_thread::sleep_for(5s);*/
//}

